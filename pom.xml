<project xmlns="http://maven.apache.org/POM/4.0.0"
                           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                           xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>de.htwsaar.minicdn</groupId>
    <artifactId>mini-cdn</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>mini-cdn</name>
    <url>https://stl-gitlab.htwsaar.de/pib-vs-wise-2526/gruppe-08-mini-cdn</url>

    <!-- Multi-Module Reactor -->
    <modules>
        <module>common</module>
        <module>origin</module>
        <module>edge</module>
        <module>router</module>
        <module>cli</module>
        <module>tests</module>
    </modules>

    <properties>
        <!-- Java -->
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Dependency versions (zentral) -->
        <spring-boot.version>4.0.0</spring-boot.version>
        <junit.version>4.13.2</junit.version>
        <picocli.version>4.7.7</picocli.version>
        <jooq.version>3.20.10</jooq.version>
        <sqlite-jdbc.version>3.45.1.0</sqlite-jdbc.version>

        <!-- Plugin versions (zentral) -->
        <spotless.version>2.43.0</spotless.version>
        <palantir.javaformat.version>2.39.0</palantir.javaformat.version>
        <maven-compiler-plugin.version>3.14.1</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.5.2</maven-surefire-plugin.version>
        <maven-failsafe-plugin.version>3.5.2</maven-failsafe-plugin.version>
    </properties>

    <!-- Zentralisiere Versionen; Module deklarieren Dependencies dann ohne <version> -->
    <dependencyManagement>
        <dependencies>

            <!-- Spring Boot BOM (Versionen für Spring/Transitives) -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Versions-Fixierung für libs, die du in Modulen brauchst -->
            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>

            <dependency>
                <groupId>info.picocli</groupId>
                <artifactId>picocli</artifactId>
                <version>${picocli.version}</version>
            </dependency>

            <dependency>
                <groupId>org.jooq</groupId>
                <artifactId>jooq</artifactId>
                <version>${jooq.version}</version>
            </dependency>

            <dependency>
                <groupId>org.xerial</groupId>
                <artifactId>sqlite-jdbc</artifactId>
                <version>${sqlite-jdbc.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <build>

        <!-- Diese Defaults erben alle Module; aktiv werden Plugins erst, wenn ein Modul sie unter <plugins> referenziert
             (oder wenn sie hier unter <plugins> stehen). -->
        <pluginManagement>
            <plugins>

                <!-- Compiler -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <source>${maven.compiler.source}</source>
                        <target>${maven.compiler.target}</target>
                        <encoding>${project.build.sourceEncoding}</encoding>
                    </configuration>
                </plugin>

                <!-- Unit Tests -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                    <configuration>
                        <useModulePath>false</useModulePath>
                    </configuration>
                </plugin>

                <!-- Integration/E2E Tests -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${maven-failsafe-plugin.version}</version>
                </plugin>

                <!-- Spotless (Format) -->
                <plugin>
                    <groupId>com.diffplug.spotless</groupId>
                    <artifactId>spotless-maven-plugin</artifactId>
                    <version>${spotless.version}</version>
                    <configuration>
                        <java>
                            <palantirJavaFormat>
                                <version>${palantir.javaformat.version}</version>
                                <style>PALANTIR</style>
                                <formatJavadoc>false</formatJavadoc>
                            </palantirJavaFormat>
                        </java>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>

            </plugins>
        </pluginManagement>


        <!-- Diese Plugins laufen in ALLEN Modulen (weil sie hier "aktiv" sind). -->
        <plugins>

            <!-- Format automatisch in verify (wenn du lieber "check" willst, sag Bescheid) -->
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>spotless-apply</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>apply</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>

</project>